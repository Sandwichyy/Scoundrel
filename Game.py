import random

cards = [] #fill w cards
health = 20

#if weapon DNE -1 else dmg value
weaponVal = -1
skip = False

room = []
for i in range(4):
     room[i] = -1

def fillRoom():
     index = random.randInt(0, 51)
     if room[0] == -1:
          #pull from card list
     if room[0] == -1:
          #pull from card list
     if room[0] == -1:
          #pull from card list
     if room[0] == -1:   
          #pull from card list


while health > 0:
     #if skip pull 4 new cards
